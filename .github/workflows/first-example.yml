name: First workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: Welcome message
        run: echo "My first GHA job" 
      
      - name: List files
        run: ls

      - name: Read file
        run: cat README.md