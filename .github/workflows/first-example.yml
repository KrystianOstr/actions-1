name: First workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: list repo
        run: ls -ltra

      - name: executing shell script
        run: |
          chmod +x ascii-script.sh
          ./ascii-script.sh